git push -u origin master

 119  git add --all
  120  git commit
  123  git push

this are the four comand u have to use.. 
git clone <repo>
git status   -- this will give file that has changed after cloning
git add -all 
git commit 'commnets'
git push origin master 

https://dont-be-afraid-to-commit.readthedocs.io/en/latest/git/commandlinegit.html


in Python Panda ..
type data.   (enter tab ) to see all attributes of the panda variable.
use shift tab to see fucntion description
